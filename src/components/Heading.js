import styled from "styled-components/macro";

const Heading = styled.h1`
  width: 100%;
  font-style: normal;
  font-weight: 800;
  font-size: 40px;
  margin: 20px 0;

  > span {
    color: #b4e0e8;
  }
`;

export default Heading;
